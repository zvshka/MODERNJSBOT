var canvas
function setup() {
  //var gui = createGui("Discord")
  //gui.addGlobals();
  tecst = createP("Craft is Life")
  tecst.position(40, -25);
  tecst.style("font-family", "monospace");
  tecst.style("color", "#FFFFFF");
  tecst.style("font-size", "30pt");
  tecst.style("padding", "10px");
  head()
}

function head() {
  canvas = createCanvas(innerWidth, 100)
}

function head2() {
  canvas = createCanvas(innerWidth, 100)
  
}

function draw() {
  background(50, 50, 50)
}